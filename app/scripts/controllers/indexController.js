app.controller('indexController', function($scope) {
    var mySwiper = new Swiper ('#topEscorts', {
        // Optional parameters
        direction: 'horizontal',
        loop: true,
        // If we need pagination
        pagination: '.swiper-pagination'
    })
});